apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ko.nodeAgentName" . }}-config
  labels: {{- include "ko.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ tpl .Values.config . | indent 4 }}
